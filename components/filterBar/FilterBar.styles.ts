import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    marginBottom: 10,
  },
  tagStyle: {
    marginBottom: 8,
    borderColor: "#fff",
  },
});
